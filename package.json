{
  "name": "qa-automation",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:smoke": "MAX_SKUS=5 playwright test",
    "install:browsers": "playwright install"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2",
    "dotenv": "^16.6.1",
    "fast-xml-parser": "^5.3.0",
    "fflate": "^0.8.2"
  }
}
